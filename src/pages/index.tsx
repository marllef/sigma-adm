import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className="flex h-full w-full items-center justify-center">
      The Page Index.tsx
    </div>
  );
};

export default Home;
